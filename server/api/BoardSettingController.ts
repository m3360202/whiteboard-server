import { Meteor } from 'meteor/meteor';
import BoardBusinessProvider from '../business/BoardBusinessProvider';

Meteor.methods({
  getBoardSetting(searchKey, offset) {
    this.unblock();
  
    // return targetList;
  },
});

